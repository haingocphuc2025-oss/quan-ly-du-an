# Browser QA Prompt

Open source and clone at identical route, viewport, zoom, state and comparable data. Capture screenshots. Check layout, typography, color, spacing, states, responsive, console and network. Record each difference with severity and reproducible evidence.
