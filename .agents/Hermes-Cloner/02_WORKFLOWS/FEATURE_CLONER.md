# Feature / Component Cloner

Dùng khi chỉ tái tạo một component hoặc một vùng UI.

`/context → /clarify → /capture-ui hoặc /ui-spec → /component-map → /tasks → /build → /review → /compare-ui → /verify`

Không tạo lại toàn bộ app shell nếu không cần.
